---
---

Add runnable core and Playwright examples for common semantic assertions, with
scripted providers by default, opt-in TypeSafe usage, setup documentation, and CI
coverage. No published package changes.
