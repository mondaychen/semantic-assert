---
"semantic-assert": minor
---

`Judge.classify` (and `PageJudge.classifyPage`) accept a per-call `template` option, matching `expectClaims`, so classification wording can be changed without touching suite settings.
